---
"@voyant-travel/legal": patch
---

Expose contract-template creation and updates as admitted MCP tools. Template creation now uses a handler-owned created-target command with server-derived idempotency, while updates declare their exact existing target.
